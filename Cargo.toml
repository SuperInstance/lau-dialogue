[package]
name = "lau-dialogue"
version = "0.1.0"
edition = "2021"
description = "NPC dialogue engine for the Lau game world"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.150"
